---
import ThemeToggle from './ThemeToggle.astro';
import { siteConfig } from '../../data/site';

const currentPath = Astro.url.pathname;
---

<header
  id="header"
  class="fixed top-0 left-0 right-0 z-40 bg-[var(--color-dark-900)] border-b border-white/5 transition-all duration-300"
>
  <nav class="container mx-auto px-6 py-4 flex items-center justify-between">
    <!-- Logo / Terminal prompt style -->
    <a href="/" class="flex items-center gap-1 hover:text-[var(--color-terminal-green)] transition-colors font-[var(--font-mono)] text-sm">
      <span class="text-[var(--color-terminal-green)]">adam</span><span class="text-gray-500">@</span><span class="text-[var(--color-terminal-cyan)]">dev</span><span class="text-gray-500">:</span><span class="text-[var(--color-terminal-cyan)]">~</span><span class="text-gray-400">$</span>
    </a>

    <!-- Navigation -->
    <div class="flex items-center gap-8">
      <ul class="hidden md:flex items-center gap-6">
        {siteConfig.navigation.map((item) => {
          const isActive = currentPath === item.link || currentPath.startsWith(item.link + '/');
          return (
            <li>
              <a
                href={item.link}
                class:list={[
                  'font-[var(--font-mono)] text-sm transition-colors relative group',
                  isActive
                    ? 'text-[var(--color-terminal-green)]'
                    : 'text-gray-400 hover:text-[var(--color-terminal-green)]',
                ]}
              >
                <span class="text-[var(--color-terminal-cyan)] opacity-60">/</span>{item.name}
                {isActive && (
                  <span class="absolute -bottom-1 left-0 w-full h-0.5 bg-[var(--color-terminal-green)]" />
                )}
              </a>
            </li>
          );
        })}
      </ul>

      <ThemeToggle />

      <!-- Mobile menu button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 text-gray-400 hover:text-[var(--color-terminal-green)] transition-colors"
        aria-label="Toggle menu"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </nav>

  <!-- Mobile menu -->
  <div
    id="mobile-menu"
    class="hidden md:hidden border-t border-white/5 bg-[var(--color-dark-900)]"
  >
    <ul class="container mx-auto px-6 py-4 space-y-4">
      {siteConfig.navigation.map((item) => {
        const isActive = currentPath === item.link || currentPath.startsWith(item.link + '/');
        return (
          <li>
            <a
              href={item.link}
              class:list={[
                'block font-[var(--font-mono)] text-lg',
                isActive ? 'text-[var(--color-terminal-green)]' : 'text-gray-400',
              ]}
            >
              <span class="text-[var(--color-terminal-cyan)] opacity-60">/</span>{item.name}
            </a>
          </li>
        );
      })}
    </ul>
  </div>
</header>

<script>
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  mobileMenuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
</script>
